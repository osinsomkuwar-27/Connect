// environment.js
// Provides the backend server URL using CRA's environment variable convention.
// Set REACT_APP_SERVER_URL in your Render environment settings.
const server = process.env.REACT_APP_SERVER_URL;

export default server;
